services:
  php:
    # comment the following line in production, it allows to have nice human-readable logs in dev
    tty: true
    # environment:
    #   SERVER_NAME: :7000
    # ports:
    #   - "7000:7000"